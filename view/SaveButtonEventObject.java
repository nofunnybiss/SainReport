package view;

import java.util.EventObject;

public class SaveButtonEventObject extends EventObject{
	// save the credentials of the Student in the controller


		public SaveButtonEventObject(Object source) {
			super(source);
		}

}